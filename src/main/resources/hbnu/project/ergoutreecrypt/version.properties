# 应用版本号，桌面端显示的唯一来源。
#
# 默认跟随 pom.xml 的 <version>：构建时由 Maven 资源过滤把下面的占位符替换成实际版本，
# 因此发版只需要改 pom.xml，主界面标题、设置与关于页会一起更新。
#
# 如需手动指定固定版本，直接把下一行改成具体版本号即可：
# 不含 ${} 的值不会被过滤替换，会原样保留。
#
# 若本文件缺失或未被过滤（例如某些 IDE 直接运行、不执行资源过滤），
# AppVersion 会回退到读取项目根目录的 pom.xml，再不行才显示 dev。
version=${project.version}
